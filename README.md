TIL - UUID versions can dramatically affect performance in MySQL

```shell
go run ./cmd/main.go --iterations 1 --version 1 --address localhost --username root --password admin --database TestDb --allow-native-passwords true
```